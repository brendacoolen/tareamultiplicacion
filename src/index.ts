let tabla:string=document.getElementById
let btn = document.getElementById("btn");
btn.addEventListener("click", () => {
function tabladel(numerodetabla :number) {
  let Rango: number;
   Rango=0;
   for (let Rango= 1; Rango<=9; Rango++) {
        let multiplicacion: number=rango*numerodetabla
   }
  if (tabla.value=== A|| tabla.value===B|| tabla.value===C|| tabla.value===D) {
tabladel(9)
      console.log ("tabladel")
  } else {
    console.log("invalido.Ingrese A B C o D")
  }